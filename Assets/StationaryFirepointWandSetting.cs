using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StationaryFirepointWandSetting : MonoBehaviour
{
    public float wandAmount;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void AssignWandAmount(float fortniteBalls)
    {
        wandAmount = fortniteBalls;
    }
}
