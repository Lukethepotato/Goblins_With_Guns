using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.InputSystem;

public class CompleteTextWithButtonPromptSprite : MonoBehaviour
{
    /*
    public static string ReadAndReplaceBinding(string textToDisplay, InputBinding actionsNeeded, TMP_SpriteAsset spriteAsset)
    {
        string stringButtonName = actionsNeeded.ToString();
        stringButtonName = RenameInput(stringButtonName);

        textToDisplay = textToDisplay.Replace(oldValue: "PROMPT", newValue: $"<sprite=\"{spriteAsset.name}\">");

        return textToDisplay;
    }

    private static string RenameInput(string stringButtonName)
    {
        stringButtonName = stringButtonName.Replace(oldValue: "Interact:", newValue:string.Empty);

        stringButtonName = stringButtonName.Replace(oldValue: "<Keyboard>/", newValue:"Keyboard_");

        stringButtonName = stringButtonName.Replace(oldValue:"<Gamepad>/", newValue:"Gamepad_");

        return stringButtonName;

    }
    */
}
